# Name of nodes to start
# here we have a single node
CELERYD_NODES="celery"

# Path to the 'celery' command. This is relative to the service's WorkingDirectory
# (ie the VariantGrid install dir) so it works wherever VariantGrid is installed, eg
# /opt/variantgrid or /mnt/variantgrid. The .venv is created by uv - see the
# "Install Python venv" wiki page
CELERY_BIN=".venv/bin/celery"

# App instance to use
# comment out this line if you don't use an app
CELERY_APP="variantgrid"

# How to call manage.py
CELERYD_MULTI="multi"

# Extra command-line arguments to the worker
# The dispatcher reads this pool's concurrency as its VEP slot count (annotation.celery_utils), so this
# is how many VEP processes run at once. Pin it rather than letting celery default to the CPU count:
# with ANNOTATION_VEP_MEMORY_LIMIT_GB bounding one run, concurrency x that limit is the pool's worst
# case, and a core count is the wrong thing for it to scale with. Raise on a box with the RAM for it.
CELERYD_OPTS="-n annotation_workers --queues=annotation_workers --concurrency=4"

# - %n will be replaced with the first part of the nodename.
# - %I will be replaced with the current child process index
#   and is important when using the prefork pool to avoid race conditions.
CELERYD_PID_FILE="/var/run/celeryd_annotation_workers/celeryd_%n.pid"
CELERYD_LOG_FILE="/var/log/variantgrid/%n%I.log"
CELERYD_LOG_LEVEL="INFO"
